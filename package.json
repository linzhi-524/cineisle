{
  "name": "cineisle-root",
  "private": true,
  "version": "0.4.7",
  "description": "CineIsle monorepo helper scripts for Railway and local server deployment.",
  "scripts": {
    "railway:build": "cd server && npm install --package-lock=false --no-audit --no-fund && npm run check",
    "railway:start": "cd server && npm start",
    "start": "npm run railway:start"
  },
  "engines": {
    "node": "20.x"
  }
}
